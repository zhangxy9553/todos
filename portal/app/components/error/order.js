var config = require("./error.config");
module.exports = {
  social : config.defaultValue
  , password : config.defaultValue
  , bankcard : config.defaultValue
  , user : config.defaultValue
  , goods : config.defaultValue
};
