var errorConfig = require("./error.config");
module.exports = {
  username : errorConfig.defaultValue
    , password : errorConfig.defaultValue
    , repeat_password : errorConfig.defaultValue
    , captcha : errorConfig.defaultValue
    , phonecode : errorConfig.defaultValue
};
